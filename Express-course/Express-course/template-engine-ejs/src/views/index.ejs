<%- include('partials/header') %>
<h1>
    <%= title %>
</h1>

<% if (isActive) { %>
    <p>The feature is active.</p>
<% } else { %>
    <p>The feature is not active.</p>
 <% } %>

<ul>
    <% users.forEach(user => { %>
        <li><%= user.name %> - <%= user.age %> years old</li>
    <% }); %>
</ul>

<%- include('partials/footer') %>